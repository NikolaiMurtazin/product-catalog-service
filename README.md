# Product Catalog Service (Маркетплейс) - ДЗ 1

Консольное приложение для управления каталогом товаров, разработанное в рамках отбора на стажировку.
Первое ДЗ реализовано с использованием **только Java Core и коллекций**.

## 🚀 Особенности и Реализованные Фичи

* **Чистая архитектура (Clean Architecture):**
    * **Model:** Модели данных (`Product`, `User`).
    * **Repository:** Интерфейсы для доступа к данным (`ProductRepository`) и их In-Memory реализации (`InMemoryProductRepository` на `ConcurrentHashMap`).
    * **Service:** Бизнес-логика (`ProductService`, `AuthService`).
    * **UI:** Консольный интерфейс (`ConsoleMenu`) с разделением на пакеты `in` (ввод) и `out` (вывод).

* **Управление Товарами (CRUD):**
    * Добавление, изменение, удаление и просмотр товаров.
    * Доступно только для `ADMIN`.

* **Поиск и Фильтрация:**
    * Реализован поиск по категории, бренду и диапазону цен с использованием `Stream API`.

* **Авторизация:**
    * Реализован вход в систему. Доступ к админ-функциям (CRUD) только у роли `ADMIN`.
    * **Логин:** `admin`
    * **Пароль:** `admin123`

* **Аудит Действий:**
    * Логируются все важные бизнес-действия (вход/выход, CRUD) с указанием пользователя и времени.

* **(БОНУС) Ускорение запросов (Кэширование):**
    * Повторные запросы **поиска/фильтрации** кэшируются.
    * При первом поиске данные берутся из "хранилища".
    * При повторном поиске с теми же параметрами — мгновенно из кэша (`Map`).
    * Кэш **автоматически сбрасывается** (инвалидируется) при любом изменении данных (добавление, изменение, удаление товара), обеспечивая актуальность.

* **(БОНУС) Метрики:**
    * Реализован замер времени выполнения поискового запроса (в мс), который наглядно демонстрирует работу кэша (мгновенный повторный ответ).

* **Тестирование:**
    * Service-слой покрыт Unit-тестами с использованием **JUnit 5** и **Mockito** для изоляции зависимостей.

* **Javadoc:**
    * Все классы, поля и методы (включая `private`) покрыты Javadoc-комментариями.

## 🛠 Стек технологий

* Java 17
* Maven (для сборки и управления зависимостями)
* JUnit 5 (для тестов)
* Mockito (для мокирования зависимостей в тестах)
* AssertJ (для "fluent" проверок в тестах)

## 🏃‍♂️ Сборка и Запуск

Проект использует Maven.

### 1. Сборка (Компиляция и Тесты)

Выполните в терминале из корневой папки проекта:

```bash
mvn clean install